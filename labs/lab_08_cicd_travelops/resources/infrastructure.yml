# LAB 08 - TravelOps
#
# File:
# resources/infrastructure.yml
#
# Purpose:
# Documents Unity Catalog infrastructure intentionally not owned by the
# Databricks Asset Bundle.
#
# Ownership:
# Terraform owns raw Volume registrations. DAB must not create, rename, replace
# or own the existing dbr_dev.parvinbadalov schema.
#
# Environment behavior:
# personal_dev and personal_prod raw managed volumes live in the personal
# Terraform root. azure_prod raw external volume lives in the azure-prod
# Terraform root.
resources: {}
