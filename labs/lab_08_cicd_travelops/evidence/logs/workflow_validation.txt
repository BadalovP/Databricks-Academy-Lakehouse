Checked: 2026-09-16

- Workflow YAML parsed successfully: 12 jobs.
- Job dependency policy assertions passed.
- LAB08_ENABLE_AZURE_OIDC=true gates every advanced Azure OIDC/Terraform job.
- LAB08_ENABLE_AZURE_OIDC=false gates one main-only PAT job using DATABRICKS_AZURE_HOST and secret DATABRICKS_AZURE_TOKEN.
- PAT mode falls back to the configured Personal workspace and current Azure storage account, so it does not require extra host or infrastructure variables beyond the supplied four-setting configuration.
- The PAT job contains no Azure CLI or Terraform command and has no id-token permission.
- PR path contains no Terraform apply, Azure PROD deploy/run, PAT job, or Personal PROD run.
- Main first promotes Personal DEV and Personal PROD, then selects either the advanced Terraform/OIDC Azure jobs or the temporary PAT deploy/run/verify job.
- The advanced Azure PROD application run is gated by explicit LAB08_RUN_AZURE_PROD_JOB=true authorization; PAT mode runs it automatically.
- Azure PROD plan/apply guards require all nine final Terraform resource addresses in remote state, including the imported RBAC assignment and application schema.
- The advanced plan/apply jobs require the imported RBAC assignment to plan exactly no-op and refuse every non-no-op fresh plan; an unchanged plan does not require roleAssignments/write.
- Both Azure Blob backend keys now contain the complete migrated states, so the workflow's remote-state inventory gates can pass without recreating resources.
- The Azure target is pinned to the existing PAT deployment root, preserves the human IS_OWNER entry, and restricts the GitHub service principal to direct CAN_MANAGE on the adopted job and pipeline objects instead of target-level propagation.
- OIDC run 35021845707 reached Azure PROD deployment with working authentication and planned only job and pipeline permission updates before the non-admin owner-change checks rejected them.
- The Azure target now omits target-level permission propagation and defines resource-scoped ACLs that preserve the human IS_OWNER model while granting the service principal CAN_MANAGE only on the exact adopted resources.
- Post-fix job and pipeline permission nodes are unchanged; remote state still reports the human as IS_OWNER and the service principal as CAN_MANAGE, without any target-level ownership reconciliation.
- A normalized LF post-fix plan is 0 add, 0 change, 0 delete, 8 unchanged.
- Azure PROD normal validation exits 0 while reporting the intentional unmanaged human CAN_MANAGE ACL on the pinned human-owned root; Personal DEV and Personal PROD retain strict validation.
- Delete-free pre-deploy plans and exact zero-change post-deploy plans remain mandatory in both Azure deployment modes.
- Terraform formatting passed for Personal, Azure PROD, and the CI backend template.
- Ruff passed.
- Black passed.
- Pytest passed: 9 tests.
- Personal DEV strict validation passed; plan is 0 add, 0 change, 0 delete, 8 unchanged.
- Personal PROD strict validation passed; plan is 0 add, 0 change, 0 delete, 8 unchanged.
- Personal Terraform detailed-exit-code plan returned 0 and No changes.
- Azure PROD Terraform validation passed; the safe fresh plan was 3 add, 0 change, 0 destroy, and the final detailed-exit-code plan returned 0 and No changes.
- The prior Azure PROD deployment passed with a final 0 add, 0 change, 0 delete plan; the current undeployed permission-as-code update remains delete-free.
- The first Azure PROD run failed safely on the missing-schema precondition. Terraform added only the application schema, and rerun 829850820027662 completed all three tasks successfully.
- Future advanced-mode Azure PROD application runs remain gated by LAB08_RUN_AZURE_PROD_JOB=true; temporary PAT mode is gated by LAB08_ENABLE_AZURE_OIDC=false.
- Service principal 141097843869075 is active with workspace and SQL entitlements, minimum DAB ACLs, and warehouse CAN_USE; no Unity Catalog or Terraform state change was required.
